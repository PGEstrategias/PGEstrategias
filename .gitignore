# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz
package-lock.json

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# email previews (generados)
/preview-out
/.react-email

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Lista Médica: analítica y evidencias de verificación.
# Las tablas de clics y búsquedas crecen en cada visita, así que no se
# versionan (si el archivo no existe, el almacén las trata como vacías).
# Las evidencias de cédula son respaldo interno y no van al repo.
/data/listamedica/clics.json
/data/listamedica/busquedas.json
/data/listamedica/evidencias/
